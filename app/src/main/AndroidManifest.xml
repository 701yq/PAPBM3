<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PAPBM3"
        tools:targetApi="31">

        <!-- MainActivity yang sudah ada -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PAPBM3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ListActivity yang sudah ada -->
        <activity
            android:name=".ListActivity"
            android:exported="true"
            android:label="List Page"
            android:theme="@style/Theme.PAPBM3" />

        <!-- Tambahkan GithubProfileActivity di sini -->
        <activity
            android:name=".GithubProfileActivity"
            android:exported="true"
            android:label="GitHub Profile Page"
            android:theme="@style/Theme.PAPBM3" />

        <activity android:name=".MainScreenActivity"/>

    </application>
</manifest>
